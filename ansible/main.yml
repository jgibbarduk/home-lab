---

- hosts: server:client
  become: yes
  roles:
    - linux_setup
  vars_files:
    - vault.yml
  tags:
    - setup

- hosts: server:client
  roles:
    - role: oh_my_zsh_setup
      users:
        - username: james
        - username: root
  vars_files:
    - vault.yml
  tags:
    - setup

- hosts: server:client
  become: yes
  roles:
    - tailscale_setup
  vars_files:
    - vault.yml
  tags:
    - setup
    - tailscale

- hosts: localhost
  roles:
    - bootstrap_hashiup
  vars_files:
    - vault.yml
  tags:
    - hashiup

- hosts: localhost
  roles:
    - nomad_cluster
  vars_files:
    - vault.yml
  tags:
    - nomad

- hosts: localhost
  roles:
    - consul_cluster
  vars_files:
    - vault.yml
  tags:
    - consul