---

- hosts: server:client
  become: yes
  roles:
    - linux_setup
  vars_files:
    - vault.yml
  tags:
    - setup

- hosts: server:client
  roles:
    - role: oh_my_zsh_setup
      users:
        - username: james
        - username: root
  vars_files:
    - vault.yml
  tags:
    - setup


- hosts: localhost
  roles:
    - bootstrap_hashiup
    - consul_cluster
    - nomad_cluster
  vars_files:
    - vault.yml
  tags:
    - nomad
    - consul


